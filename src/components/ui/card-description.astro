---
import { cn } from "@/utils";

interface Props {
  className?: string;
}

const { className = "", ...rest } = Astro.props;
---

<div
  data-slot="card-description"
  class={cn("text-sm font-base", className)}
  {...rest}
>
  <slot />
</div>
