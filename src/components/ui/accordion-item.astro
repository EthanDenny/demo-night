---
interface AccordionItemProps {
  value: string;
  className?: string;
}

const { value, className = "" } = Astro.props;
---

<div
  data-accordion-item
  data-value={value}
  class={`rounded-base overflow-hidden border-2 border-b border-border shadow-shadow ${className}`}
>
  <slot />
</div>

