---
import { buttonVariants } from "./buttonVariants";
import { cn } from "@/utils";

interface Props {
  variant?: "default" | "noShadow" | "neutral" | "reverse";
  size?: "default" | "sm" | "lg" | "icon";
  type?: "button" | "submit" | "reset";
}

const {
  variant = "default",
  size = "default",
  type = "button",
  ...rest
} = Astro.props;

const classes = cn("cursor-pointer", buttonVariants({ variant, size }));
---

<button
  data-slot="button"
  class={classes}
  type={type}
  {...rest}
>
  <slot />
</button>
